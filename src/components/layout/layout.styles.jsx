import styled from 'styled-components';

export const LayoutContainer = styled.div`

    .main-body {
        display: flex;
        flex: 1;
        overflow: hidden; 
        height: calc(100vh - 5rem);
    }
`