import styled from 'styled-components';

export const QuestionContainer = styled.div `
    .question-answer {
        border: 1px solid black;
        border-radius: 10px;
        cursor: pointer;
    }
`