export const selectCurrentUser = (state) => state.user.currentUser;

/*  a selector function named selectCurrentUser that extracts the 
currentUser data from the application's state. It's often used in conjunction 
with state management libraries to access specific parts of the application's 
state in React components or other parts of the application */
